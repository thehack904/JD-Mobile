# JD-Mobile host bind mount location (TrueNAS recommended)
# Example: /mnt/tank/apps/jd-mobile/config
JD_MOBILE_HOST_CONFIG_DIR=/mnt/tank/apps/jd-mobile/config

# Web UI port on the host
JD_MOBILE_PORT=8086

# Optional: Override config path inside container (default shown)
JD_MOBILE_CONFIG_PATH=/app/config/config.json
